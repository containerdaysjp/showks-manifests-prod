---
# Source: showks-canvas/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: showks-canvas-yustoris
  namespace: showks
  labels:
    app: showks-canvas-yustoris
    class: showks-canvas
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: app-port
  selector:
    app: showks-canvas-yustoris

---
# Source: showks-canvas/templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: showks-canvas-yustoris
  namespace: showks
  labels:
    app: showks-canvas-yustoris
    class: showks-canvas
spec:
  replicas: 1
  selector:
    matchLabels:
      app: showks-canvas-yustoris
  template:
    metadata:
      labels:
        app: showks-canvas-yustoris
        class: showks-canvas
    spec:
      containers:
        - name: showks-canvas-yustoris
          image: gcr.io/ipc-containerdays/showks-canvas-yustoris:prod-847d0cfcab059e7d1999cd71ff366fd855e9f305
          ports:
            - name: app-port
              containerPort: 8080
              protocol: TCP

---
# Source: showks-canvas/templates/ingress.yaml
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: showks-canvas-yustoris
  namespace: showks
  labels:
    class: showks-canvas
  annotations:
    kubernetes.io/ingress.class: "nginx"
    nginx.org/ssl-services: "showks-canvas-yustoris"
    ingress.kubernetes.io/ssl-redirect: "true"
spec:
  tls:
    - hosts:
      - showks-canvas-yustoris.prod.showks.containerdays.jp
      secretName: tls-certificate
  rules:
  - host: showks-canvas-yustoris.prod.showks.containerdays.jp
    http:
      paths:
      - path: /
        backend:
          serviceName: showks-canvas-yustoris
          servicePort: 8080

